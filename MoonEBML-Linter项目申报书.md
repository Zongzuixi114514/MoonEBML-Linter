# MoonEBML-Linter 项目申报书

**2026 年 9 月 MoonBit 黑客松**

## 基本信息

- 项目名称：MoonEBML-Linter
- 参赛者：Zongzuixi114514
- 联系方式：1247095691@qq.com
- 项目地址：https://github.com/Zongzuixi114514/MoonEBML-Linter
- 项目方向：基础软件和开发工具 / 多媒体基础设施
- 许可证：MIT License

## 项目简介

Matroska（`.mkv`）和 WebM（`.webm`）是常见的媒体容器格式，底层使用 EBML 编码。它们的数据结构是嵌套的，字段长度也不是固定的，手动排查文件问题并不方便。

MoonEBML-Linter 是一个用 MoonBit 编写的 Matroska/WebM 解析和检查工具。项目希望让 MoonBit 开发者可以直接读取媒体容器的结构，查看音视频轨道信息，并定位格式错误。

我选择这个题目，是因为 MoonBit 目前缺少面向多媒体容器的基础库。这个项目既能练习 MoonBit 的二进制数据处理，也有机会成为以后媒体工具、文件校验和 CI 检查的底层组件。

## 已完成的工作

目前项目已经完成了第一版解析链路：

- EBML 可变长度整数（VINT）和元素头解析；
- 不复制数据的 payload 视图和有边界的元素遍历；
- 无符号数、有符号数、浮点数、UTF-8 和 UTC 时间解码；
- EBML 元素目录，记录元素 ID、名称和数据类型；
- Matroska `Segment`、`Info`、`Tracks`、`TrackEntry` 解析；
- 视频轨道的宽高、显示尺寸、帧率等字段解析；
- 音频轨道的采样率、声道数和位深解析；
- 截断数据、非法长度、重复字段和缺失字段等错误处理。

仓库中目前有 73 个测试，均已通过 `moon test`。代码也会通过 `moon fmt` 和 `moon check` 检查。

## 技术思路

项目分成三个主要部分：

1. `wire/ebml`：只负责从字节中读取 EBML 基础结构；
2. `catalog/ebml`：维护 Matroska 元素的定义；
3. `container/matroska`：把底层元素转换成音视频领域中更容易使用的结构。

解析过程中保留元素的原始偏移，并尽量使用视图而不是复制 payload。这样遇到错误时，可以指出具体的元素和字节位置，也方便以后处理较大的媒体文件。

## 黑客松阶段计划

接下来准备完成两个能直接演示的功能：

- 诊断引擎：把解析错误整理成规则编号、严重级别、元素位置和修复建议；
- 命令行入口：输入一个 `.mkv` 或 `.webm` 文件，输出文件摘要、轨道信息和发现的问题。

同时会增加一些真实文件和异常文件夹具，用来验证未知长度元素、截断文件、重复字段等情况。Cluster、Cues 和更多编码器私有数据会作为后续扩展。

## 项目特点

- 使用 MoonBit 原生类型和测试体系，没有把 JavaScript 或其他语言解析器作为核心依赖；
- 底层 EBML 解析和上层 Matroska 规则分开，今后也可以支持其他 EBML 文档；
- 错误信息带有元素 ID 和字节偏移，便于命令行、CI 或编辑器继续使用；
- 先完成小而可靠的解析能力，再逐步增加校验规则，方便持续迭代。

## 预期成果

黑客松结束时，希望仓库能够提供：

- 可被其他 MoonBit 项目引用的 EBML/Matroska 解析 API；
- 一个可以实际检查媒体文件的命令行示例；
- 一组覆盖正常和异常输入的测试夹具；
- 清晰的 README、使用示例和后续开发路线。

项目最终会继续在 GitHub 上开源维护，并在接口稳定后考虑发布到 Mooncakes。
